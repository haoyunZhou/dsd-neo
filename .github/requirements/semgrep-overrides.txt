# Semgrep 1.171.0 pins mcp==1.23.3, which is affected by multiple high-severity advisories.
# Standard Semgrep scans do not use its MCP server integration, so force the patched SDK until Semgrep updates its pin.
mcp>=1.28.1
